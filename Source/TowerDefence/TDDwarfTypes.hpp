#pragma once

enum DwarfType {
	Middle = 0,
	Little = 1,	
	Big = 2
};

enum DwarfAwardType {
	LittleAward = 5,
	MiddleAward = 7,
	BigAward = 20
};

enum DwarfHealthType {
	LittleHP = 20,
	MiddleHP = 30,
	BigHP = 45
};

enum DwarfMovementSpeedType {
	LittleSpeed = 150,
	MiddleSpeed = 100,
	BigSpeed = 50
};

enum DwarfDamageType {
	LittleDamage = 10,
	MiddleDamage = 15,
	BigDamage = 25
};

